import { OAuthController } from 'src/controllers/oauth.controller';
import { WebhookController } from 'src/controllers/webhook.controller';

export const controllers = [OAuthController, WebhookController];
